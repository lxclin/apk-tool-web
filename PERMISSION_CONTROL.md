# APK Tool 本地权限控制

## 目标

分发源码、压缩包或 Git 仓库时，所有私有功能默认关闭。授权文件必须由所有者电脑上的签名私钥生成，并绑定目标电脑；修改旧的本地 JSON 开关、复制其他电脑的授权文件或篡改授权内容都不会获得权限。

当前已实际保护的功能：

- 候选 CP 筛选与自动指派（界面隐藏 + 执行入口二次校验）

已经预留的权限项：适配后台自动提交、批量自动化适配、Asana 自动写入、批量清理设备应用。为避免改变现有手动流程，这些预留项暂未拦截原按钮；后续可按需逐项接入。

## 所有者使用

- 所有者签名私钥仅位于 `~/.apk-tool-owner/ed25519-private.pem`，不在项目和 Git 中。
- 当前电脑授权位于 `~/.apk-tool/license.json`，也不在项目和 Git 中。
- 在所有者电脑的主窗口按 `Command + Shift + L` 打开隐藏的“所有者权限配置”。Windows/Linux 调试环境可用 `Ctrl + Shift + L`。
- 输入目标电脑完整设备 ID、授权对象、有效天数及允许功能，生成授权 JSON 后传给使用者。

## 使用者安装授权

- 在非所有者电脑的工具主窗口按 `Command + Shift + L` 打开隐藏的“设备授权”窗口。同一个快捷键会根据电脑是否持有所有者私钥自动分流。
- 复制完整设备 ID 发给所有者。
- 收到签名授权 JSON 后，在同一窗口选择“安装授权文件”，重启工具生效。

## 安全边界

签名授权可以阻止普通使用者通过修改配置、复制授权文件或伪造 JSON 解锁功能。但如果把完整源码和后台凭证都交给具备开发能力的人，对方仍可能删除本地校验代码或直接调用后台接口。需要不可绕过的强权限时，应把敏感操作迁移到由所有者控制的服务端，并在服务端校验账号、设备和权限；客户端只负责发起请求。
