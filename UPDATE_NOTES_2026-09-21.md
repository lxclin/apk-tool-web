# 2026-09-21 迭代记录

本文记录 2026 年 9 月 21 日完成并验证的自动适配、闪退识别、广告参数提取和批量进度优化。

## 1. 闪退与进程退出规则化识别

### 修复内容

- 新增 `crash_rules.py` 和 `crash_rules.json`，将历史日志中的常见问题固化为可版本化规则。
- 支持识别并提取：
  - Java 缺少运行时类或依赖不完整。
  - Native 库缺失、ABI 不匹配。
  - 线程资源不足（`pthread_create`、无法创建新线程等）。
  - Java OOM、Native signal、Java/API 二进制不兼容、安全异常和反射异常。
  - ANR、应用启动被系统阻止、任务被移除、系统回收进程等生命周期事件。
- 只有命中目标包体并具备有效崩溃证据时才判定为包体闪退；任务被移除、系统回收或启动阻止不会误报为闪退。
- Asana 预检评论增加规则编号、原因码、证据指纹和未知异常模式。
- 相同状态但证据指纹发生变化时允许新增历史评论，避免不同根因被去重吞掉。

## 2. 自动化启动与设备 UI 稳定性

- `uiautomator dump` 超时后自动重试一次。
- 首启弹窗、通知权限和 MAX Debugger UI 读取共享一次 UI 快照，减少重复 dump 和超时概率。
- UI 读取失败时记录兼容性警告并继续安全流程，不再直接中断整个自动化任务。
- MAX Debugger 辅助提取遇到 UI 超时返回结构化失败结果，并向界面输出可操作原因。

## 3. 运行期广告 ID 兜底识别

### 触发门禁

运行期日志兜底不是所有缺少 ID 的任务都会触发，必须同时满足：

1. 聚合类型为已支持平台：AdMob、MAX、IronSource、LevelPlay、TopOn 或 Fyber。
2. 归因为 Adjust，或 AppsFlyer 且 `af_key` 完整。
3. 插屏和激励视频两个广告 ID 同时缺失。

只缺失其中一个 ID 时不启动该策略，避免把运行期猜测混入已有的标准检测结果。

### 识别规则

- AdMob 使用 Provider、全屏 Activity、广告位曝光日志进行保守关联。
- 其他平台要求同一日志行同时包含平台标记、广告类型以及明确的 `adUnitId`、`ad_unit_id` 或 `placementId` 字段，才允许自动回填。
- 证据不足的 ID 只保留为“运行时候选广告 ID”，不会直接提交后台。
- 记录识别来源和置信度，Asana 及失败评论中使用通用的“运行时候选广告 ID”字段。

## 4. 批量自动适配实时进度

- GUI 增加总体进度条和实时进度文本。
- 显示当前处理序号、包名、当前阶段、已完成数量、成功、闪退、启动失败、自动化失败和其他归因统计。
- 将 VPN 检查、包体安装、ADB 配置、日志检测、参数校验、Asana 回填、后台提交、广告回放等阶段同步到进度区域。
- 暂停、继续、安全停止和队列完成时保留明确的当前阶段状态。

## 5. Asana 与自动化状态调整

- 无目标包体崩溃证据的进程退出改为“启动待复检”，避免直接写入“包体闪退”。
- 自动化失败及回放相关结果保留结构化规则信息和日志证据，便于历史复盘。
- 临时通过 `video/inter` 推断为 IronSource、但广告回放失败后，撤销占位聚合结果并重新执行 Google Play 下载量与疑似白包判断，避免低下载量包体误落入“未识别聚合类型”。
- 构建配置已把 `crash_rules.json` 纳入各 GUI、Internal、Standard、Web 和 ADB Proxy 打包目标。

## 6. 非游戏空参数提交与高概率 CP 重算

- Native、Flutter、ReactNative、Capacitor 等非游戏应用，在聚合类型为空或插屏/激励 ID 同时为空时，不再只记录自动化失败；工具会清空旧适配参数，并通过接口分别提交备注“聚合类型识别为空”或“聚合id识别为空”。
- 非游戏空参数策略优先于疑似白包规则，避免低下载量覆盖更准确的应用类型结论；Unity、Cocos2d-x 和应用类型未知的包体保持原流程。
- 高概率 CP 模型改为读取全部年度/月度历史工作表，同时统计 rain 与 snow，按包名保留最新结案结果并排除历史 TradPlus 不支持造成的失真失败。
- 实际重算得到 2,362 个去重有效样本，整体适配率 57.6%；游戏/益智为 78.2%，普通包名 47.7%，社交/媒体 58.9%，奖励/赚钱 56.8%，博彩/老虎机 56.3%，工具/清理 38.0%，日本包体 7.1%。
- “高概率”阈值由 55% 提高至 70%，避免把仅接近总体均值的类别误选；新增最少 12 个历史样本的包名词评分，用可靠的细分类历史信号补充大类评分。
- 后台候选数据中的 `categ=GAME` 现在作为明确游戏分类证据参与评分，修复此前字段只展示、不参与计算，导致包名不带 `game/puzzle` 的真实游戏被误归为普通包名的问题。

## 7. 聚合类型为空但存在广告 ID 的临时 MAX 策略

- AutoDetector 未识别聚合类型，但存在 AppLovin SDK Key、至少一个有效广告 ID、有效 Activity，且归因等必填参数通过校验时，临时按 MAX 聚合回填并提交后台。
- 临时 MAX 使用中置信度业务规则标记，必须经过真实广告回放验证；只有回放成功才保留 MAX 参数。
- 回放失败或异常时撤销临时 MAX 结论、保留真实检测到的广告 ID、清空后台适配参数，并在 Asana 描述和评论中记录“聚合类型为空，但存在广告id”。
- 没有 AppLovin SDK Key 的“仅存在广告 ID”结果不会触发 MAX 推断，避免把 AdMob、IronSource 等未知来源 ID 误提交为 MAX。

## 验证

- 定向测试：`316 passed`。
- 全量测试：`752 passed`。
- 本次未上传 APK 导出目录、ZIP 包、Playwright 临时文件等运行产物。

## 8. G99 二次闪退终局策略

- 仅在批量自动适配明确运行于 G99，且命中目标包真实 `APP_CRASHED` 证据时触发；普通进程退出、切后台、系统回收、启动未确认和设备异常不会触发。
- G99 再次闪退后，直接清空旧适配参数并通过接口提交“闪退，g99也闪退，暂不适配”，不再继续聚合检测和广告回放。
- Asana 描述和评论同步记录判定结果、崩溃原因、规则编号、证据指纹及关键闪退日志；任务状态归档为“包体闪退”。
- 批量进度与断点结果将该终局计入“包体闪退”，不会误计为一般自动化失败或疑似白包。

## 仍待后续优化

部分包体需要点击页面或进入特定业务页面后才会触发广告。当前自动回放主要负责重启应用并监听日志；如果没有触发动作，应该区分“广告未触发，无法验证”和“广告回放失败”，并为这类包体补充 UI 操作脚本。

## 9. 9 月 22–23 日批量队列与待人工任务调整

- “待处理”“待人工检查”和“待人工”统一纳入批量自动适配候选队列，普通设备与 G99 均适用；已完成、已加黑、聚合适配成功等终态任务仍不重复入队。
- 普通设备处理上述任务时，如果包体未安装，先通过 APKCombo 自动下载安装，再进入 ADB 配置、聚合检测、后台校验和广告回放；安装失败仍安全退回待处理，避免空跑一轮自动适配。
- G99 原有的安装失败、启动失败和包体闪退重装策略保留；“待人工”不再只对 G99 放行。
- “批量预检并自动适配”改为忽略搜索框当前选中行，从整张任务列表扫描待预检任务，修复只预检最后选中任务、导致前序待处理任务直接进入适配的问题。
- 批量自动适配按钮文案改为“批量自动适配（含待处理/待人工）”，使实际入队范围与界面提示一致。
- 设备实测中，`com.indiez.train.miner` 已完成安装、聚合检测和后台校验，但两轮广告回放均未捕获真实广告展示；`com.belkagames.mc` 完成参数检测后因 Logcat 监听进程提前结束而终止，均保留待复核状态，不伪造适配成功。

## 10. 本轮验证

- 普通设备“待处理 / 待人工检查 / 待人工”自动补安装：3 组参数化测试通过。
- 待处理入队、G99 兼容策略、组合入口忽略搜索选中行、界面按钮文案及安装流程回归测试通过，共 7 项定向测试通过。
- 未纳入版本库：APK 导出目录、ZIP 包、Playwright 临时目录等运行产物。
