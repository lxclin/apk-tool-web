# APK Tool 历史迭代与近期更新记录（截至 2026-09-03）

## 2026-09-03 手机 VPN 检测与 Mac 代理转发处理记录

适用场景：目标应用检测 Android VPN 接口并弹出 `VPN Detected`，但测试又必须使用 VPN 网络。

本次验证有效的临时方案：停止手机端 Clash/VPN，保持 Mac 的 Clash Verge 和 VPN 正常运行；使用 ADB USB 反向端口转发，将手机的 `127.0.0.1:7897` 转到 Mac 的 `127.0.0.1:7897`，再把手机全局 HTTP 代理临时设为 `127.0.0.1:7897`。这样 Android 不会创建 `tun0 / TRANSPORT_VPN`，而支持系统代理的 HTTP/HTTPS 流量仍从 Mac 的 VPN 出口访问。本次已在 `com.bird.cash.clicker.money.tap.earn.wood` 上验证：VPN 检测弹窗消失；经 Google Play 更新来源认证后，应用正常进入主界面。

必须执行的收尾操作：测试结束或 USB 断开前，清除手机全局代理并移除 ADB 反向转发。USB 转发会在连接中断后消失，但手机全局代理会继续保留；如果代理仍是 `127.0.0.1:7897` 而 `adb reverse --list` 已无对应转发，Google Play 和其他 Google 服务会因连接到空端口而无法联网。恢复命令如下：

```bash
adb shell settings put global http_proxy :0
adb reverse --remove tcp:7897
```

恢复后应同时确认：手机代理显示 `:0`、ADB 反向转发列表为空、所需的手机 VPN 网络状态为已连接且已验证；必要时强制停止后重新打开 Google Play。该代理方案主要覆盖遵循 Android 系统代理的 HTTP/HTTPS 请求，完全忽略系统代理或直接使用 UDP 的应用不保证适用。

本次事故处理：昨日 USB 转发失效后，手机仍残留 `127.0.0.1:7897` 全局代理，导致 9.3 页面预检无法取得 Play Store 信息，并将 6 个任务错误标记为 `UNKNOWN / 待人工检查`。现已清除残留代理，并精确删除这 6 条错误 Asana 评论；`com.nianticlabs.monsterhunter`、`air.com.gan.orc.real`、`com.lego.membership`、`gametester.io`、`com.vaultnallsfrenzy.vbfplinko.slots`、`com.brain.puzzle.tricky.test.annoying.quest` 均已恢复为“待处理”，可重新进入批量预检。

## 2026-09-02 日报补齐结构化失败终态

日报不再要求所有失败评论必须包含“暂不适配”文字。自动化重试结束后产生的 `AF_KEY_EMPTY`、`LOGCAT_ENDED` 和 `AUTOMATION_FAILED` 结构化评论会分别归入“af_key为空缺失”“聚合回放失败”和“自动化适配失败”，统一计入聚合适配问题；同一包当天后续重试成功时，仍由较新的成功评论覆盖早先失败，避免重复统计。

使用 9.2 Asana 真实数据验证：当前分组 23 个任务全部归类，8 个聚合适配成功、15 个聚合适配问题，未归类数量由 4 降为 0；全量 597 个测试通过。

## 2026-09-02 af_key 缺失状态细分

自动化明确返回 `AF_KEY_EMPTY` 时，今日任务列表不再使用笼统的“参数待确认”或“自动化失败”，而是直接显示“af_key为空”。即时检测结果、批量适配结果以及下次从 Asana 评论恢复任务状态时保持一致；其他参数缺失仍沿用原有状态，不受本次调整影响。

验证：覆盖即时列表状态、其他失败状态隔离、结构化及历史 Asana 评论恢复；全量 593 个测试通过。

## 2026-09-02 今日 Asana 任务包名即时搜索

页面预检的“今日 Asana 任务”区域新增包名搜索框：输入完整或部分包名时立即匹配，自动滚动到命中行、选中该任务，并同步带入下方“GP 链接或包名”检查框。精确匹配优先于模糊匹配；多个结果可按回车或点击“下一个”循环定位，状态区显示当前位置和匹配总数，按 Esc 可清空搜索。搜索选择按人工选中处理，后续批量操作会尊重当前定位行。

验证：覆盖完整包名即时定位、部分包名多结果循环、选中行与下方输入框同步；全量 587 个测试通过。

## 2026-09-02 低下载量疑似白包自动终态

新增“疑似白包，暂不适配”业务规则：聚合复检完成后，如果聚合类型仍为空，或已识别聚合类型但插屏、激励视频聚合 ID 均为空，工具会读取该包 Google Play 官方详情页的公开下载量。下载量显示下限低于 10 万（例如 `5+`、`50+`、`10,000+`、`50,000+`）时，自动判定为“疑似白包，暂不适配”；`100,000+` 及以上不使用此规则，仍保留原参数待确认结论。页面不可访问或下载量无法解析时也不会猜测，继续沿用原检测结果。

完整处理链路：

1. 在界面参数区、自动化日志和 Asana 描述中保留 Google Play 下载量与聚合检测证据。
2. 写入结构化 Asana 业务评论 `SUSPECTED_WHITE_PACKAGE`；即使聚合类型为空，也允许回填检测结果和“疑似白包，暂不适配”终态。
3. 通过适配接口清空聚合参数，只提交备注“疑似白包，暂不适配”，随后刷新缓存并回读确认；Asana 回填或后台提交任一步失败都不会伪装成已完成，任务会保留以便重试。
4. 提交成功后写回 Rain 的 Sheet 暂不适配结果、跳过广告回放，并在批量统计中单独计入“疑似白包”，随后继续下一条任务。

补充修复：Google Play 新页面会把 `1K+` 与 `Downloads` 分别放在相邻 HTML 标签中，旧解析仅支持同一段纯文本，导致低下载量规则未触发。现已兼容带标签页面及 `0+`、`1K+`、`10K+`、`1.5M+` 等缩写。实测 `com.astrolabecraft.PlayOptics` 为 `1K+`、`com.zyya.puzzlevxwsmn.vexashiftqzd` 为 `0+`，均会进入疑似白包终态。

验证：覆盖下载量解析、缩写和 HTML 标签格式、10 万边界、聚合类型为空、聚合类型存在但广告 ID 为空、Asana 无聚合类型终态回填、接口提交/刷新、Sheet 回写和批量状态恢复；全量 590 个测试通过。

## 2026-09-02 AndroidManifest 归因兜底识别

背景：部分包体运行时日志与 AutoDetector 无法输出归因平台。此前只能由人工执行 `adb shell pm path <包名>`、找到 `base.apk`，再下钻 `AndroidManifest.xml` 搜索归因 SDK；即使 Manifest 仍无归因，也缺少统一的自动化终态。

本轮改动：

1. 聚合类型与广告 ID 已提取、但动态日志没有归因平台时，自动进入 Manifest 兜底；设备、日志或聚合检测本身异常时不会触发，避免掩盖真正的运行故障。
2. 自动查询已安装包路径，并严格选择 `/base.apk`（不误选 `split_config.*.apk`）；临时拉取后使用本机 Android SDK 的 `aapt2/aapt` 解码 `AndroidManifest.xml`，完成即清理临时文件。
3. Manifest 静态特征目前可识别 AppsFlyer、Adjust、AppMetrica、SolarEngine、Singular、Tenjin、ThinkingData、Airbridge、Kochava 和 Branch，并保存命中的类名/配置项作为识别依据。
4. 识别到 Adjust 或 AppsFlyer 后继续沿用现有参数完整性规则；识别到其他归因平台时沿用“其他归因，暂不适配，只提交备注并跳过回放”；Manifest 也未命中任何已知归因 SDK 时，明确记录为“未知归因，暂不适配”。
5. 自动化适配的单次提取、失败复检和批量任务三条入口全部接入；界面参数区、自动化日志及 Asana 描述会展示“AndroidManifest.xml 兜底”及识别依据，便于复核来源。

验证：新增 base.apk 精确选择、Manifest 多平台识别/排除误报、字段展示、Adjust 恢复及未知归因终态等回归测试；界面层 108 个测试、全量 574 个测试全部通过。并用当前连接设备上的 `com.captchatyping.onlinejobapp.jobwork.jobworkonline` 完成真实读取，Manifest 未命中已知归因平台，符合“未知归因，暂不适配”的预期。

## 2026-08-31 预检队列纳入“待处理”任务，堵住无法预检的死角

背景：任务从 Asana 读取后，如果当天没有进入“新增待预检”集合（例如前一天读取过但从未预检），会一直停留在“待处理”状态；而旧的预检队列构建逻辑只收“新增待预检 / APKCombo有包”，且无新增任务时直接返回空队列——“待处理”任务永远进不了预检，也没有资格进入自动适配，成为死角。

修改（`gui.py` `_precheck_batch_queue_from_selection`）：

1. “批量预检今日待处理”与“批量预检并自动适配”共用的队列构建逻辑现在同时纳入“待处理”行：待处理 → 页面预检 → 下载/安装 → 启动检查 → 自动适配全链路可走通；无任何新增任务的场景也不再直接返回空队列。
2. “批量自动适配预检合格任务”（`_automation_eligible_precheck_tasks`）行为不变：仅接受“启动正常 / 启动待复检 / 安装完成 / 已安装”，G99 额外接受“安装失败 / 包体闪退 / 待人工检查 / 待人工”，任何情况下都不直接接收“待处理”，避免绕过广告、地区限制和启动检查。
3. 保留原有逃生门：手动选中某个旧行时，仍从该行起全部重新预检。

测试：`tests/test_gui.py` 新增 3 个回归测试（无新增任务时待处理行入队、待处理与新增任务同批入队且合格状态不混入、组合入口对待处理行先预检再衔接自动适配），并锁定待处理行不得进入合格任务适配队列（普通设备与 G99 双验证）；全量 545 个测试通过。

## 2026-08-27 代码去重：本地代理复用 ADB 核心层

本地代理 `adb_proxy.py` 不再维护自己的 ADB 实现，改为直接引用 `adb_pusher.py` 核心层（1355 → 1233 行）：

1. 删除重复实现并改为核心层引用：`run_stream` 流式执行、`check_device` 设备检测、六个 ADB 命令构建器（push_config / zygote_build / get_uid / clear_cache / force_stop / open_app）、logcat 启停、zip/XAPK 内容检测，以及 UID 提取。删除未被调用的 `af_key` 提取副本。
2. 顺带修复了两处长期漂移：代理的 `zygote_build` 此前不修复 zygotehole 权限，现在与桌面版一致，构建后自动串联 chmod/chown；代理的 `clear_cache` 此前只执行 `pm clear`，现在与桌面版一致，先 force-stop 再 `pm clear`。
3. `adb_pusher.zip_contains_apks` 提升为公共函数（旧内部名保留别名）；删除了 `start_logcat_stream` 中无人读取的 `_filter_pattern` 死代码。
4. 新增 `tests/test_adb_proxy.py`（15 个测试）：用同一性断言锁定代理符号必须直接引用核心实现，防止将来复制粘贴回潮后再漂移；并覆盖 UID 单行提取等价性和 zygote_build / clear_cache 命令内容。

验证：全量 493 个测试通过；代理真实启动冒烟（WS 初始推送、force_stop 命令链路、HTTP 回填接口、进程干净退出）全部正常。

## 2026-08-27 底层稳定性修复

本轮聚焦运行时资源与并发问题，不改变任何业务逻辑和界面：

1. 修复 `run_stream` 子进程泄漏：`on_line` 回调抛异常（如界面已销毁、WebSocket 发送失败）时，原实现会直接中断且不回收子进程，长期运行会累积孤儿 adb 进程占住 stdout 管道；现在 `finally` 中兜底 kill 并 wait，桌面/Web 共用的 `adb_pusher.py` 与代理的 `adb_proxy.py` 副本同步修复。
2. 看门狗超时终止逻辑加固：超时提示日志回调抛异常不再阻止后续 kill，进程不会再因回调异常而残留。
3. 错误通知回调加保护：命令不存在或执行报错时的 `[错误]` 消息回调自身再抛异常，不会再导致线程提前死亡、`on_done` 丢失。
4. Web 版 `server.py`：`/api/config` 读写、`/api/device`、`/api/get-uid`、`/api/adb-path`、`/api/whitelist`、`/api/upload-apk` 共 7 个含阻塞 IO 的端点由 `async def` 改为同步 `def`，交给 FastAPI 线程池执行；任何一个 ADB/文件操作卡顿都不会再冻结整个服务（含全部 WebSocket）。
5. WebSocket 命令流队列轮询 `q.get(timeout=0.1)` 改为 `asyncio.to_thread`（`server.py` 1 处、`adb_proxy.py` 2 处）：原先每次轮询都会阻塞事件循环约 100ms。
6. logcat 启停（`logcat -c`、Popen、terminate/wait 最长 3 秒）和 `adb devices` 设备检查全部移入线程池执行（`server.py` 的 `logcat_start`/`logcat_stop`/`device_check`/连接初始推送，及 `adb_proxy.py` 对应分支）；logcat 停止时先置空全局引用再异步回收，避免并发窗口内引用被覆盖。
7. 新增回归测试 `TestRunStreamProcessCleanup`：验证回调抛异常后子进程被回收、命令不存在时 `on_done` 仍正常触发；全量 478 个测试通过。

兼容性说明：本轮全部为底层修复，REST/WebSocket 接口行为与界面不变。

## 2026-08-26 稳定性迭代（v1.4.0）

本轮按自动化稳定性、结果准确性和可恢复性完成以下建设：

1. 自动化队列增加原子化断点，进程或设备异常后可恢复当前包体，不覆盖未完成队列。
2. 每个包体执行前检查 ADB 授权、唯一设备、存储空间、配置、构建脚本、安装状态和目标目录。
3. APK/XAPK 安装不再只相信电脑端返回值；会以手机包管理器、UID 和启动 Activity 做最终验收，兼容“电脑提示超时但手机实际已安装”。
4. 普通日志缺少 MAX 广告 ID 时，可自动调用 Mediation Debugger Share，读取分享文本并按出现顺序提取插屏/激励 ID。
5. 每个包体生成本地结构化 JSON 执行报告，记录阶段、字段快照、重试和最终结果，默认保留 30 天。
6. 推送配置、构建、启动应用和后台接口采用分层重试；仅重试 ADB 瞬时断连、超时和短暂网络错误。
7. 桌面版与 Web 版共用预检流程引擎，统一后台提交、下载资格、评论结果和任务状态规则。
8. 建立统一版本号与发布检查；桌面标题及 Web `/api/version` 显示源码/打包运行方式，减少旧包误用。
9. TradPlus 纳入正常聚合类型：支持识别对应广告 ID、接口提交 `tradplus` 平台参数，并按 TradPlus 日志执行广告回放验证；不再按“其他聚合，暂不适配”处理。

## 最近业务规则与体验优化

### 自动化适配

- 自动化检测固定使用 `mediation_test_snow`，每个包体都会重新写入包名和 UP2 appid、推送配置、执行构建、重新获取 UID，并先停止上一包体的日志监听。
- 聚合参数提取改为等待 AutoDetector 完整终态，避免应用刚启动便被误判为“聚合类型为空”；字段缺失会重启后复检一次。
- 聚合类型、AppsFlyer Key、插屏 ID 或激励 ID 二次仍缺失时，停止后台适配并将明确原因写入 Asana。
- IronSource 的标准 `video`/`inter` 组合可作为中置信度临时推断；回放期间若检测到明确 MAX 结果，会先清空旧 IronSource 参数，再提交 MAX 参数并二次回放。
- 多广告 ID 支持逐个轮换：已经成功的广告类型会锁定当前 ID，失败的类型继续尝试下一个 ID；每轮监听 300 秒。
- 普通回放监听默认 500 秒，严格以展示、Impression、Reward 或 Revenue 事件作为成功依据，仅加载成功不计为回放成功。
- 回放日志按实际聚合平台过滤，支持 MAX、IronSource、LevelPlay、AdMob 和 TradPlus，减少无关 SDK 日志导致的延迟和误判。
- 运行中出现通知权限弹窗会自动处理；偶发 ANR 会持续选择“等待”，由整体检测或回放超时控制最终结果，避免把短暂卡顿直接认定为闪退。
- 首启隐私协议、语言选择、教程和游客登录等低风险弹窗支持按控件文字安全处理；购买、订阅、付款页面不会点击，广告页的通用“Skip”也不会误触。
- 切换到下一个任务时会清空上一任务的聚合字段，防止旧参数残留或异步结果串包。

### 后台提交与业务终态

- 聚合参数改为接口提交，并在提交后自动清除 A2 缓存、回读后台数据进行真实生效校验。
- 后台缓存清理兼容本机 CA 证书链异常，不再因证书发行链缺失而直接失败，同时不会关闭 HTTPS 校验。
- 非 AppsFlyer/Adjust 的其他归因不再标成“自动化失败”：仅提交备注“归因为 xxx，暂不适配”，回填 Asana 并跳过广告回放。
- “全网无包”和预检加黑结论会自动提交后台并刷新缓存；全网无包使用备注字段，加黑使用 Block 原因字段。
- 未识别聚合类型但检测到 IronSource `video`/`inter` 时，可先临时提交并回放；回放失败会清空此前参数，只保留“未识别出聚合类型，暂不适配”。
- rain 的聚合回放成功、其他归因暂不适配等明确业务终态会尽力回写 Google Sheet 的聚合进度、完成时间和结果字段；Snow 和其他适配人员的行保持只读，ADB 超时等瞬时技术失败也不写入。

### 页面预检与下载安装

- 今日任务支持增量读取和评论恢复状态，不再把已处理问题任务重新重置为待处理；列表顺序按 Asana 建立日期递减规则保持一致。
- 批量预检可从当前选中行开始，也可只处理本次新增任务；符合条件的已安装包体可直接进入自动化适配队列。
- Google Play 地区或设备限制时会自动核对 APKCombo；完全匹配包名且有可用版本时进入第三方下载安装，无匹配结果才判定全网无包。
- 安装状态以手机包管理器、UID 与启动 Activity 二次验收，修复电脑端超时或报错但手机实际已安装仍被标记“安装失败”的问题。
- 大包下载不会阻塞整个队列；后台下载中的任务会延后复查，其他包体继续处理。
- 首次启动异常会自动重试一次，第二次结果正常则继续适配；支持识别已安装应用的 32 位/64 位 ABI 与实际运行进程位数。
- “高概率 CP”不再只使用固定包名分数：优先读取 Sheet 中 Snow 已完成的历史聚合结果，按包名类别动态计算成功率并做小样本校正；旧的 TradPlus 不支持记录不参与评分，读取失败时自动退回内置基准。
- 高概率筛选窗口合并低概率/快速加黑候选：接口中“应用内广告=NO”的记录默认勾选；游戏名称和大分类均为空、且应用内付费/广告均为 NO 的信息缺失记录自动排除。

### Asana、日报与界面

- 聚合结果自动写入 Asana 描述中 GP 链接下方，同时保留包名、UP2 appid 和 GP 链接；失败或终态原因写入评论。
- 日报统一使用“完成 N 个聚合适配”，并细分全网无包、归因问题、闪退、聚合类型/广告 ID 缺失、登录限制和加黑原因。
- 修复“动作适配成功”等表达未被日报统计、其他聚合名称与包名粘连、首条问题前缺少换行等问题。
- 自动化日志展示完整 ADB 前置、参数检测、后台提交及回放过程；底部安装/执行状态栏固定可见。
- 自动化页可从预检选中任务带入包名、UP2 appid 和 Task GID，并一键同步写入 ADB 配置。

## 兼容性说明

- 原有 ADB 手动按钮、自动化脚本、数据同步与手动后台跳转保持不变。
- 断点和执行报告属于本机运行数据，已排除在 Git 提交之外。
- 版本数据结构均带 schema 版本；不兼容的旧断点不会被强行恢复。

---

## 历史完整记录：2026-08-04 ～ 2026-08-25

> 以下内容由原完整迭代文档原样并入，保留当时的流程说明、验证结果和边界记录，便于追溯。


更新周期：2026-08-04 ～ 2026-08-25
适用版本：APK Tool 1.3（桌面版、Web 版、本地代理）

### 一、本轮迭代概述

本轮工作的核心，是把原来依赖测试人员逐个点击 ADB 按钮的聚合适配流程，逐步改造成可批量执行、可恢复状态、可自动提交并能验证真实广告回放的自动化流程。

本轮主要完成了以下能力：

1. 新增 Google Play 页面预检及批量下载流程。
2. 新增聚合参数自动检测、Asana 回填、后台接口提交、缓存刷新和回放验证流程。
3. 新增 Asana 当日总结及问题原因分类。
4. 加强 ADB、Logcat、UID 切换、闪退判断和长时间任务的稳定性。
5. 同步桌面版、Web 版和本地代理能力。
6. 保留所有原有手动按钮和手动操作逻辑，自动化使用独立流程调用底层能力。
7. 打通 APKCombo 真实文件解析、自动下载、浏览器兜底和安装结果确认。
8. 增加多广告 ID 轮换回放，并修复渐进式日志造成的聚合检测误判。
9. 将全网无包结论纳入后台提交、缓存刷新和回读确认闭环。
10. 第三方包批量清理增加逐包进度和失败统计。

---

### 二、界面和模块调整

#### 2.1 顶部 Tab 调整

当前顶部模块顺序已整理为：

1. APK 工具
2. 页面预检
3. ADB 指令
4. 自动化脚本
5. 自动化适配
6. 当日总结
7. 数据同步

ADB 指令页仍保留原有配置、推送、构建、UID、Logcat 和参数提取按钮；自动化适配页不会改变这些手动按钮的原有行为。

#### 2.2 新增页面预检 Tab

用于读取当天 Asana 包体任务，并在真正适配前完成 Google Play 页面检查、下载和启动检查。

#### 2.3 新增自动化适配 Tab

用于完成 ADB 前置配置、聚合检测、Asana 参数回填、后台提交、缓存刷新和广告回放验证。

#### 2.4 新增当日总结 Tab

用于读取当天 Asana 任务描述及评论，生成聚合适配、动作适配、暂不适配和加黑结果汇总。

---

### 三、Google Play 页面预检

#### 3.1 当天 Asana 任务读取

- 只读取日期与当天一致的 Asana 执行区段，例如 `8.14执行`。
- 从任务描述提取包名、UP2 appid 和 Google Play 链接。
- 按 Asana 当前任务顺序显示，避免 GUI 顺序与 Asana 不一致。
- 支持一天内多次读取增量任务。
- 重新读取时同步检查任务评论，恢复真实状态，不再把已经处理过的问题任务统一重置为“待处理”。
- 已有终态评论的任务会恢复为已加黑、暂不适配、闪退、后台失败、回放失败或聚合适配成功等状态。
- 支持从当前选中的列表项开始批量预检，而不是强制从第一条重新开始。
- 支持只处理本次新增在列表顶部的任务，减少重复预检历史任务。

#### 3.2 Google Play 页面识别

页面预检优先读取手机 UI 控件；UI 信息不足时，才使用内存截图 OCR。正常截图不会落盘保存，避免产生大量缓存文件。

当前可识别：

- 包含广告（Contains ads）
- 仅应用内购（In-app purchases）
- 设备不支持
- 当前账号所在国家或地区不支持
- Item not found / Google Play 无包
- 页面未稳定、暂时无法判断
- 日本包体

地区或设备限制页面即使没有展示“包含广告/应用内购”标签，也会优先识别为限制状态，不再误判成“未发现广告或应用内购”并继续触发无效下载。

#### 3.3 当前广告标识判断规则

判断优先级调整为先判断是否有广告，再判断能否下载：

- 标注“包含广告”：允许继续下载和适配。
- 只标注“应用内购”、没有广告：加黑。
- 既没有广告也没有应用内购：不加黑，仍然下载，交由后续人工检查。
- 同时存在设备不兼容和仅应用内购时：优先按“应用内购、无广告”加黑。
- Item not found：Asana 回填“Google 无包/全网无包，暂不适配”。
- 包名含独立 `jp` 段，并且 Google Play 页面存在明显日文内容：按日本包体加黑。

#### 3.4 预检加黑自动提交后台

预检判定为加黑后，不再只更新 Asana 状态，而是自动执行：

1. 查询 CP 后台现有包体记录。
2. 保留后台已有聚合参数。
3. 仅更新对应的 `Block原因`。
4. 调用后台接口提交。
5. 清除当前包名的 A2 缓存。
6. 回读后台并确认加黑原因已真实生效。
7. 成功后列表显示“已加黑(后台)”。

“全网无包，暂不适配”现已纳入同一后台闭环：保留原后台聚合字段，仅写入对应 `block_ps`，随后刷新 A2 缓存并回读确认。提交失败会在桌面版和 Web 版明确显示，不会误报为处理完成。

提交、清缓存或回读任一步失败时，会显示“加黑提交失败”，并把具体失败原因写入 Asana 评论，不会误报成功。

#### 3.5 自动下载和队列处理

- “检测到广告后自动下载安装”默认开启。
- 默认单轮下载上限由 4 调整为 6。
- 默认下载间隔为 60 秒。
- 大包下载不会长时间阻塞整个列表；程序会先继续预检并发起后续包体下载。
- 达到单轮下载上限的任务不会丢弃，而是标记为延后下载。
- 所有页面检查完成后，会再次处理达到上限或尚未完成下载的任务。
- 支持停止批量预检。
- 下载暂停、登录验证、安装失败等状态会明确展示，不会把队列错误显示为“全部完成”。
- 大包下载改为后台并行等待：某个 2GB 包体尚未安装时，程序继续发起后续包体下载；所有下载发起后再统一轮询已安装包体并执行启动检查。
- 再次读取今日任务、开始批处理前会核对设备安装状态，把已经完成安装但仍显示“后台下载中”的任务自动刷新为“安装完成”。
- 任务状态列加宽，`APKCombo待确认`、`已加黑(后台)`、`后台下载中` 等完整状态不再被截断。

#### 3.6 安装后的启动检查

- 安装完成后自动启动应用并观察进程。
- 检查打开立即闪退和打开一段时间后闪退两类问题。
- 闪退需要结合进程退出和 Java/Native 崩溃证据判断，降低误报。
- 确认闪退后在 Asana 评论“包体闪退，暂不适配”。
- 未闪退时自动停止当前应用，再处理下一条任务。

#### 3.7 APKCombo 核验与自动下载安装

过去地区限制、设备不支持或 Google Play 无包时，需要人工打开 APKCombo 判断是否还有第三方下载版本。现在预检会自动完成以下核验：

1. 用完整 GP 链接和包名打开 APKCombo 搜索页。
2. 要求搜索结果存在完全一致的包名，避免把相似应用误认为目标包体。
3. 继续检查 APKCombo 下载页和下载数据，确认不是 `application was not found`，并且存在真实可下载版本。
4. Google Play 无法下载且 APKCombo 也无包时，判定“全网无包，暂不适配”。
5. APKCombo 存在目标包体时，显示“Google Play 无法下载，但 APKCombo 有包”，交由第三方包体流程继续处理。
6. 网络异常、页面结构变化或证据不足时显示“APKCombo 待确认”，不自动误判全网无包。

在确认 APKCombo 有完全一致包名后，新增自动安装链路：

1. 从下载页解析短时有效的 `/d` 或 `/r2` 真实文件地址。
2. 下载请求携带来源页信息，并在写入前校验内容确实为 APK/XAPK ZIP 文件，避免把风控 HTML 当成安装包。
3. 脚本下载遇到 403、Cloudflare 或内容校验失败时，自动改用本机 Chrome 打开下载地址。
4. 监听浏览器下载目录，下载完成后移动到临时目录并通过 ADB 安装。
5. 安装结束后必须在手机 PackageManager 中确认目标包名，发现包名不一致时停止后续自动化。
6. ADB 安装超时但手机可能仍在提交安装会话时，继续轮询最终安装状态，避免把已经安装成功的大包误报失败。
7. 历史“安装失败/APKCombo 有包”状态会迁移成可执行的中间状态，重新进入自动下载队列，而不是永久跳过。

#### 3.8 增量任务和预检+适配一体化

- 按项目 GID 和日期记录当天已经读取的 Asana Task GID。
- 一天内多次点击“读取今日任务”时，仅新增加的任务标记为“新增待预检”，旧任务保留评论恢复状态或当前会话状态。
- 新增“批量预检并自动适配”按钮：先执行页面识别、自动下载和启动检查，再把本轮合格且已经安装的任务按列表顺序移交自动化适配队列。
- 不符合条件、仍在后台下载或需要人工确认的任务不会错误进入自动适配。

---

### 四、聚合自动化适配

#### 4.1 每个包体的 ADB 前置流程

批量任务切换到每个新包体时，都会独立重新执行：

1. 写入当前包名和 UP2 appid 到 `config.json`。
2. 强制将 `taskUUID` 设置为 `mediation_test_snow`。
3. 推送 Config。
4. 执行 `zygote_build`。
5. 获取当前包名对应的应用 UID。
6. 停止上一包体的 UID Logcat 过滤。
7. 使用新 UID 重新启动 Logcat 过滤。
8. 启动当前游戏并等待 AutoDetector 输出。

该流程避免了 A 包 UID 监听残留到 B 包、配置仍使用上一包体、直接启动导致检测不到参数等问题。

#### 4.2 聚合参数检测等待策略

- 不再在应用刚启动、日志尚未生成时立即判断“聚合类型为空”。
- 首次检测正常至少等待约 60 秒。
- 日志仍在持续增长时，最长等待约 90 秒。
- 首次必要字段为空时，自动重启游戏再检测一次。
- 第二次结果尚未完整时，继续进行宽限轮询。
- 已经出现 SDK Key 或聚合 ID、但没有输出综合判断时，标记为“检测结果未完整输出”，不再误报为完全没有聚合类型。

以下字段支持一次重启复检：

- 最终判断
- Appsflyer SDK Key / af_key
- 插屏聚合 ID
- 激励视频聚合 ID

两次检测仍缺失时，才在 Asana 评论对应的参数为空或聚合类型为空。

`暂未找到`、`暂未提取到`、`暂未检测到` 与原有的“未找到/未提取到”一样统一视为空值，不允许作为 af_key、SDK Key 或其他后台参数提交。即使聚合类型可通过 `video/inter` 推断为 IronSource，AppsFlyer 的 af_key 缺失和非白名单归因仍具有更高阻断优先级，不能被推断规则绕过。

检测结果会以最终字段快照重新计算一次结论，解决 Logcat 边读取边增长时“较早返回不完整、最终字段其实已齐全”的竞态。反过来，如果最终仍缺 af_key、广告 ID 或完整判断，已检测到的有效聚合证据会先回填 Asana 描述，但不会提交适配后台。

#### 4.3 IronSource 特殊规则

如果 AutoDetector 最终判断为空，但同时满足：

- 激励视频聚合 ID 为 `video`
- 插屏聚合 ID 为 `inter`

则可以按业务规则推断为 IronSource 聚合并允许提交。

对于 IronSource/LevelPlay，`video` 和 `inter` 是合法的符号型广告位，不会被当成无效 ID。

如果临时 IronSource 配置完成后回放失败，程序会通过接口把刚提交的聚合平台、归因、广告 ID、SDK Key 和 Activity 全部置空，只保留“未识别出聚合类型，暂不适配”备注，并同步更新 Asana 描述和评论。

#### 4.4 聚合识别证据和置信度

聚合结果增加：

- 识别方式
- 识别置信度
- 识别依据
- 自动提交策略

当前规则：

- AutoDetector 明确输出最终判断：高置信度。
- 通过 IronSource `video/inter` 业务规则推断：中置信度。
- 只有零散 SDK Key 或广告 ID，没有完整判断：低置信度，禁止直接提交。

#### 4.5 Asana 聚合参数自动回填

检测完成后，自动把聚合参数写入 Asana 描述的 GP 链接下方，并保留原有：

- 包名
- UP2 appid
- GP 链接

回填内容包括最终判断、Activity、应用类型、SDK Key、插屏 ID、激励视频 ID、归因平台、识别方式、置信度和判断依据。

#### 4.6 后台参数接口提交

原有“跳转后台”手动按钮继续保留；自动化适配改为通过接口提交，避免依赖浏览器页面中的“提交”按钮。

自动化后台提交依次执行：

1. 调用 `s10_package_info` 写入聚合参数。
2. 校验写入接口返回值。
3. 调用 `delete_a2_package_cache` 清除当前包名缓存。
4. 调用 `cp_adapt/list` 回读后台记录。
5. 对包名、聚合类型、归因、广告 ID、SDK Key 和 Activity 等字段逐项校验。

只有提交、清缓存和回读全部成功，才认为后台参数真实生效。

“未找到”“未提取到”等占位内容不会作为真实参数提交。

#### 4.7 非白名单归因平台的最新处理规则

允许正常回放适配的归因白名单为：

- AppsFlyer
- Adjust

对于 SolarEngine、Singular、Tenjin、ThinkingData、Airbridge 等不包含 AppsFlyer/Adjust 的归因，当前流程为：

1. 在 Asana 评论“XXX归因，暂不适配”。
2. 仍然把完整聚合参数回填至 Asana 描述。
3. 仍然通过接口提交适配后台。
4. 清除 A2 缓存并回读确认。
5. 标记“归因暂不适配”。
6. 不进行广告回放。
7. 批量模式继续处理下一个包体。

如果归因结果中同时包含 AppsFlyer 或 Adjust，例如 `SolarEngine, AppsFlyer`，仍按白名单归因继续正常适配和回放。

#### 4.8 聚合广告回放验证

- 回放监听默认由 200 秒调整为 500 秒。
- 可配置范围为 10～600 秒。
- 后台参数生效后，强制停止并重新启动应用。
- 清空旧 Logcat，再按当前应用 UID 监听。
- 根据检测出的聚合类型，重点查看：
  - `ZGSDK max`
  - `ZGSDK level`
  - `ZGSDK AdMob`
  - `ZGSDK iron`
- 只验证实际配置了 ID 的广告类型。
- 如果同时配置插屏和激励视频，则两种广告都必须分别确认成功。

以下只代表请求或加载，不算播放成功：

- `loadAd`
- `showAd`
- `onAdLoaded`
- `display_start`

以下可作为真实展示成功证据，并且必须匹配当前广告 ID 和广告类型：

- `onAdDisplayed`
- `onAdImpression`
- `display_success`
- `onAdRevenuePaid`
- 激励确认事件

#### 4.9 多广告 ID 轮换回放

- AutoDetector 对同一广告类型输出多个候选 ID 时，后台每次只提交一个实际待验证 ID，不再把逗号分隔列表直接写入后台。
- 首轮同时检查插屏和激励视频；某一种广告成功后立即锁定该 ID，后续轮次只切换和验证尚未成功的广告类型。
- 每次切换候选 ID 都会重新回填 Asana、提交后台、刷新缓存并执行下一轮回放。
- 候选全部耗尽后明确列出仍未确认展示的广告类型。
- 单 ID 包体继续沿用原有一次回放流程，避免无意义地增加等待时间。

#### 4.10 回放 Logcat 稳定性

- 使用独立读取线程持续排空 Logcat，避免 AppLovin 等 SDK 的大量初始化日志堵塞管道。
- 判定器读取完整 UID 日志，界面只显示与当前广告 ID、展示结果和错误有关的重点日志。
- 增加 ADB 在线状态检查和短暂超时重试。
- 瞬时 Logcat 超时不直接判定业务失败。
- 持续的 ADB/Logcat 基础设施故障会停止批量队列，并且不会错误写入“包体适配失败”的业务结论。
- 每次应用重启成功后重新计算 25 秒进程启动宽限，并检查主进程、包名前缀子进程和前台 Activity，避免 force-stop/启动命令耗时挤占宽限或慢启动应用被误判。
- 自动识别 Android 通知权限弹窗并点击“不允许”，避免弹窗挡住游戏加载、聚合检测或广告回放。

#### 4.11 回放期间 IronSource → MAX 动态纠正

针对首次只能取得 `video/inter`、后续 AutoDetector 才输出完整 MAX 结果的包体，增加聚合类型动态升级机制：

1. 首次识别或推断为 IronSource 时，仍按现有规则回填 Asana、接口提交并开始第一次回放。
2. 第一次回放期间继续监听同一应用 UID 的 `ZGSDK.AutoDetector` 日志。
3. 只有 AutoDetector 明确输出 MAX、字段完整且达到高置信度时才触发切换；普通 `ZGSDK.Max`、AppLovin 初始化或广告瀑布日志不会误触发。
4. 发现 MAX 后终止第一次回放，先通过接口清空旧 IronSource 参数并刷新缓存。
5. 用新的 MAX 聚合类型、SDK Key、插屏 ID、激励视频 ID、Activity 和归因参数覆盖当前检测结果。
6. 替换 Asana 描述中的旧聚合参数块。
7. 再次通过接口提交 MAX、清缓存并回读验证。
8. 重启游戏并执行第二次 MAX 聚合回放。

如果清空旧 IronSource 参数失败，自动化会立即停止并写入明确失败原因，防止后台残留两套混合配置。

#### 4.12 批量自动化控制

- 支持按页面预检列表顺序处理所有符合条件的已安装包体。
- 每个包体完成后自动切换到下一包体。
- 自动化日志会同步显示 ADB 配置、构建、UID、检测、提交和回放过程。
- 支持“停止”和“暂停队列”。
- 停止请求会在当前不可中断的提交/清缓存/回读原子步骤安全结束后生效。
- 停止后不会继续进入广告回放。

---

### 五、Asana 状态和当日总结

#### 5.1 Asana 评论幂等处理

- 页面预检、自动化失败、后台失败、回放失败和成功评论均带结构化状态代码。
- 相同终态不会重复写入多条相同评论。
- 再次读取今日任务时，根据最新有效评论恢复 GUI 状态。

#### 5.2 当日总结生成

新增当日总结能力，读取指定日期执行区段下的任务和评论，自动归纳：

- 完成聚合适配
- 完成动作适配
- 聚合适配问题
- 动作适配问题
- 暂不适配
- 加黑

暂不适配和加黑进一步按原因分类统计，例如：

- 归因问题
- 未识别聚合类型
- 聚合广告 ID 缺失
- 聚合回放失败
- 包体闪退
- Google 无包/全网无包
- 设备或地区不支持
- 需要 Google 登录
- 应用内购无广告
- 日本包体
- 其他人工备注问题

生成结果支持一键复制。

近期又补充了以下识别和排版规则：

- 人工评论写“动作适配成功”或“动作适配完成”均计入动作适配成功，避免漏掉已完成包体。
- TradPlus 等其他聚合类型改为输出“检测出 XX 聚合，暂不适配”，避免聚合名称与包名直接粘连。
- “其他原因”继续按人工原始原因细分，不再只给出笼统计数。
- 加黑原因单独统计应用内购无广告、Google 登录、下载前置资源过长及其他人工原因。
- 问题分类汇总与首个包名之间增加空行，提升日报可读性。

#### 5.3 数据同步父任务地址自动解析

- 数据同步页支持直接粘贴 Asana 任务 URL 并点击“解析填充”。
- 对 inbox 链接优先提取 `/item/<task_gid>`，忽略后面的 `/story/<story_gid>`。
- 自动保存父任务 GID，后续新建适配任务会挂到正确的母任务下。

---

### 六、APK 下载和安装工具优化

#### 6.1 APKCombo 跳转

- Google Play 链接点击“APKCombo 下载”时，改为打开 APKCombo 搜索结果页。
- 不再错误拼接为不存在的固定 APK 详情页或 downloader 地址。
- 对纯包名输入增加有效性判断，避免点击无反应。

#### 6.2 APK/XAPK/拆分包安装

- 支持本地 APK。
- 支持 XAPK。
- 支持拆分 APK 目录。
- 自动优先选择 `base.apk`。
- 多 APK 使用 `adb install-multiple`。
- 支持识别扩展名不准确、但实际内容为 ZIP/XAPK 的文件。
- 延长大体积拆分包安装超时。
- 对 `INSTALL_FAILED_INVALID_APK: Split null was defined multiple times` 等错误给出明确安装失败信息。
- 安装命令返回超时或异常后，以手机 PackageManager 的最终包名状态再次确认，降低大包和拆分包的安装误报。

#### 6.3 第三方包清理进度

- 保留原有预览、白名单和当前包名自动保留逻辑。
- 批量清理由一次性长命令改为后台逐包卸载。
- 界面实时显示已处理数量、总数量、成功数和失败数。
- 单个包卸载失败不会中断后续包体，结束后统一列出失败包名和系统返回原因。

---

### 七、桌面版、Web 版和代理同步

本轮相关能力已同步到：

- 桌面 GUI：`gui.py`
- Web 页面：`static/index.html`
- Web 服务：`server.py`
- 本地 ADB 代理：`adb_proxy.py`

桌面版和 Web 版共用 Google Play 预检、AutoDetector 解析、后台提交、缓存刷新和回放判定规则，降低两端结果不一致的问题。

---

### 八、当前自动化主流程

```text
读取当天 Asana 任务
    ↓
Google Play 页面预检
    ├─ 仅应用内购 / 日本包体 → Asana 评论 → 后台加黑 → 清缓存 → 跳过
    ├─ Google 无包 / 设备地区限制 → APKCombo 精确包名及下载版本核验
    │                                  ├─ APKCombo 无包 → 全网无包 → Asana 评论 → 跳过
    │                                  └─ APKCombo 有包/待确认 → 保留第三方下载或人工处理
    └─ 有广告或需人工确认 → 下载、安装、启动闪退检查
                                      ↓
写 config（包名、appid、mediation_test_snow）
    ↓
推送 Config → zygote_build → 获取当前 UID → 切换 UID Logcat
    ↓
启动游戏 → 聚合参数检测（必要时重启复检）
    ↓
回填 Asana 描述
    ↓
接口提交后台 → 清除 A2 缓存 → 后台回读校验
    ├─ 非白名单归因 → 评论暂不适配 → 跳过回放 → 下一包
    └─ AppsFlyer / Adjust → 重启游戏 → 聚合广告回放验证
                                      ├─ IronSource 回放中明确识别 MAX
                                      │      → 清空 IronSource → 覆盖提交 MAX → 二次回放
                                      ↓
                    成功或失败结果写入 Asana → 下一包
```

---

### 九、验证和交付状态

本轮最终回归结果：

```text
424 passed
```

本轮代码已完成全量自动化回归。macOS 打包产物需要在发布新版时重新构建并做一次连接真实设备的冒烟验证：

- `dist/APK Tool.app`
- `dist/APK Tool Web.app`
- `dist/APK Tool Proxy.app`

---

### 十、当前边界和后续可继续优化项

当前未纳入全自动化的部分：

- 动作适配仍以人工操作为主。
- MAX Mediation Debugger 分享文本已完成轻量 Android Share Receiver 原型和保留白名单，但自动点击 Share、读取分享文本并自动回填首个 INTER/REWARDED ID 尚未接入主流程。
- Google Play 登录、验证码、账号风控等场景仍需要人工介入。
- Google Play 页面无广告标识但应用实际存在广告的情况，当前保留下载并人工检查，避免错误加黑。

---

## 更早迭代记录：2026-06-17

> 以下内容由早期 `ITERATION_SUMMARY.md` 并入，保留桌面版、Web 版、本地代理及安装链路的早期变更。


更新时间：2026-06-17

### 本轮迭代目标

围绕聚合参数提取、后台跳转 URL、Web/GUI 行为一致性、XAPK 安装链路和本地代理稳定性做了一轮集中修复。

### 主要变更

#### 1. 后台跳转 URL 格式调整

- 后台 URL 已调整为 hash 路由格式：
  `http://data_center_web_internet.hongdinghe.cn/#/CpAdaptManage/CpAdapt?...`
- 跳转参数包含 `change=1`、`package_name`、`aggr_platform`、`aggr_chaping_id`、`aggr_jilishipin_id` 等字段。
- 插屏聚合 ID 和激励视频聚合 ID 跳转时只取首个值，避免后台不支持多值导致识别失败。
- “一键复制全部”仍保留全部提取值，不受跳转单值规则影响。

#### 2. 聚合平台与聚合 ID 提取修复

- 修复了聚合 ID 被后续 SDK 段落覆盖的问题。
- 现在会先按 `ZGSDK.AutoDetector` 日志中的 SDK 段落收集聚合 ID，再根据 `最终判断` 选择对应平台：
  - `max聚合` 取 AppLovin/MAX 段落 ID
  - `IronSource聚合` 取 IronSource 段落 ID
  - `LevelPlay` 取 LevelPlay 段落 ID
  - `AdMob` 取 AdMob 段落 ID
- 对示例日志，`最终判断: max聚合` 时会正确提取：
  - 激励视频聚合 ID：`b5a21c21da9780f9`
  - 插屏聚合 ID：`caa8fdbbdf51c161`

#### 3. af_key 提取与回填

- 增加 `af_key` 提取能力。
- 支持从 `af_key`、`AppsFlyer SDK Key`、`AppsFlyer Developer Key` 等日志字段中识别。
- Web 展示、复制和后台跳转参数中都已接入。

#### 4. Web 端能力同步

- Web 端聚合参数提取通过本地 `adb_proxy.py` 代理执行。
- `adb_proxy.py` 已复用 GUI 相同的 `parse_autodetector_fields()` 解析逻辑，保证 Web 和 GUI 提取结果一致。
- GitHub Pages 页面此前已部署到：
  `https://lxclin.github.io/apk-tool-web/`

#### 5. UID 提取增强

- “获取 UID” 兼容 `dumpsys package` 输出中的 `userId=` 和 `appId=` 两种格式。
- 解决部分游戏点击获取 UID 未识别的问题。

#### 6. Google Play 打开方式调整

- GUI 中 Google Play 链接改为优先使用：
  `market://details?id=<package>`
- 并通过 `-p com.android.vending` 指定 Play Store 打开，减少先跳 WebView 再跳 Play Store 的情况。

#### 7. GUI 展示优化

- 主窗口默认尺寸调整为 `900x820`，最小尺寸为 `820x640`。
- 顶部按钮拆为两行，避免按钮名称被截断。
- 聚合参数提取结果弹窗默认尺寸加大，底部按钮固定可见。

#### 8. XAPK/APK 安装链路修复

- 支持选择本地 `.xapk` 文件安装。
- 支持选择拆分 APK 目录安装，自动递归收集 APK，并优先安装 `base.apk`。
- 单个 APK 使用 `adb install -r`，多个 APK 使用 `adb install-multiple -r`。
- `.xapk` 会解压后安装内部 APK，并推送 OBB 文件。
- 下载直链支持 `.apk/.xapk`，并兼容带 query 参数和大小写扩展名，例如：
  `https://example.com/game.XAPK?token=abc`
- 如果下载文件临时保存为 `.apk`，但内容实际是包含 APK 的 XAPK/ZIP，会自动按 XAPK 处理。
- “APKCombo 下载”按钮现在可以处理本地 APK/XAPK 路径、拆分目录、下载直链和 Google Play 地址。

#### 9. 拖拽安装功能撤回

- 曾尝试接入 `tkinterdnd2` 实现拖拽安装。
- 由于 macOS Apple Silicon + Python 3.13 + Tk 8.6 环境下 `tkdnd` 兼容性不稳定，按当前需求已撤回该功能。
- 项目不再依赖 `tkinterdnd2`，避免 GUI 启动受拖拽组件影响。

#### 10. 本地代理同步

- `adb_proxy.py` 中 XAPK 文件判断改为大小写不敏感。
- 代理下载后安装时也会识别带 query 的 XAPK 下载链接。
- 代理下载到无准确扩展名的临时文件后，会检查 ZIP 内容是否包含 APK，必要时按 XAPK 安装。

### 验证情况

已执行并通过：

```bash
python3 -m py_compile main.py gui.py adb_pusher.py adb_proxy.py server.py
pytest tests/test_main.py tests/test_gui.py tests/test_adb_pusher.py -q
```

当前测试结果：

```text
42 passed
```

本地 Web 代理 `adb_proxy.py` 已确认监听：

```text
ws://localhost:9527
```

### 涉及的主要文件

- `adb_pusher.py`
- `adb_proxy.py`
- `gui.py`
- `main.py`
- `requirements.txt`
- `static/index.html`
- `tests/test_adb_pusher.py`
- `tests/test_gui.py`
- `tests/test_main.py`
